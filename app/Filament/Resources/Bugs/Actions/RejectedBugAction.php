<?php

namespace App\Filament\Resources\Bugs\Actions;

use App\Enums\BugStatus;
use App\Models\Bug;
use Filament\Actions\Action;
use Filament\Notifications\Notification;

class RejectedBugAction extends Action
{
    public static function make(?string $name = null): static
    {
        return parent::make($name); // TODO: Change the autogenerated stub
    }

    protected function setUp(): void
    {
        parent::setUp(); // TODO: Change the autogenerated stub

        $this
            ->label('Reject Bug')
            ->color('danger')
            ->icon('')
            ->requiresConfirmation()
            ->modalHeading('Mark Bug as Rejected')
            ->modalDescription('Are you sure you want to mark this bug as rejected? This will move it to pending confirmation.')
            ->modalSubmitActionLabel('Yes, Mark Rejected')
            ->visible(fn (?Bug $bug) => $bug && ! $bug->is_paid && in_array($bug->status, [BugStatus::UNDER_REVIEW, BugStatus::CLOSED, BugStatus::SUBMITTED, BugStatus::FIXED, BugStatus::DUPLICATE]))
            ->action(function (Bug $bug) {
                $bug->update([
                    'status' => BugStatus::REJECTED,
                    'duplicate_of_id' => null,
                    'base_amount' => 0,
                    'final_amount' => 0,
                ]);
                Notification::make()
                    ->title('Bug Marked as Rejected')
                    ->body("Bug #{$bug->bug_no} has been marked as rejected.")
                    ->success()
                    ->send();
            });
    }
}
